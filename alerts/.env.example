# ============================================================================
# Raydium Alerts Configuration
# ============================================================================
# Copy this file to .env and modify as needed:
#   cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# RPC Configuration (Required)
# ----------------------------------------------------------------------------
# WebSocket endpoint for real-time block subscription.
# Use a paid RPC provider for production (Helius, QuickNode, Triton, etc.)
# Public endpoints have rate limits and may miss events during high traffic.

RPC_WS_URL=wss://api.mainnet-beta.solana.com/

# ----------------------------------------------------------------------------
# Market Filter (Optional)
# ----------------------------------------------------------------------------
# Which Raydium markets/protocols to monitor.
# Valid values: cpmm, clmm, amm_v4 (case-insensitive)
# Aliases for amm_v4: ammv4, amm-v4, v4
#
# Examples:
#   FILTER_MARKETS=clmm              # Only CLMM
#   FILTER_MARKETS=cpmm,clmm         # CPMM and CLMM
#   FILTER_MARKETS=amm_v4            # Only AMM V4
#   FILTER_MARKETS=                  # All markets (default)
#
# Default: empty = all markets (cpmm, clmm, amm_v4)

FILTER_MARKETS=

# ----------------------------------------------------------------------------
# Token Filter (Optional)
# ----------------------------------------------------------------------------
# Filter swaps by token mint addresses (comma-separated).
# Only swaps involving these tokens will be logged.
#
# Common tokens:
#   SOL (Wrapped): So11111111111111111111111111111111111111112
#   USDC:          EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
#   USDT:          Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB
#
# Example:
#   FILTER_TOKENS=So11111111111111111111111111111111111111112,EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
#
# Default: empty = all tokens

FILTER_TOKENS=

# ----------------------------------------------------------------------------
# AMM/Pool Filter (Optional)
# ----------------------------------------------------------------------------
# Filter swaps by AMM/pool addresses (comma-separated).
# Only swaps on these specific pools will be logged.
#
# Example:
#   FILTER_AMMS=poolAddress1,poolAddress2
#
# Default: empty = all AMMs/pools

FILTER_AMMS=

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------
# Log levels: error, warn, info, debug, trace
# Use debug/trace for troubleshooting, info for production

RUST_LOG=info
